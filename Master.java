public class Master{



public Directions askGoogleDirectonsAPI(){
  //FIND WHICH WORKER IS RESPONSIBLE, according to ((StartLan, StartLon),(EndLan,EndLon))
}

public Directions searchCache(String xx ){

    // search the Cache

    if (query not found in cache)) {

    }

   //hash for SourceLatLng + DestinationLatLng

}

/*

  initialize();

  waitForNewQueriesThread()

  searchCache(String);

  distributeToMappers()

  waitForMappers();

  ackToReducers();

  collectDataFromReducers();

  askGoogleDirectonsAPI();

  updateCache();

  updateDatabase();

  sendResultsToClient();

*/
}
